import React from "react";
import "./MainContainer.css";

const MainContainer = () => {
    return (
        <div>
            <h1>This is the app (for now... )</h1>
        </div>
    );
};

export default MainContainer;
